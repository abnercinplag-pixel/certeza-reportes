CERTEZA — pendientes que requieren accion o decision de Abner
Actualizado: 2026-09-24

=== 2026-09-24: adaptador de PDF, falta verificarlo con el PDF real ===
A. En esta sesion gcloud storage cp pidio aprobacion y nadie la dio: no
   pude bajar el PDF real de Cabana Emma. Las pruebas pasan con un PDF
   sintetico del mismo acomodo, pero eso NO confirma los 88/11 reales.
   Correr (baja el PDF, lo lee y falla si no da 88 conceptos y 11 capitulos):
     cd ~/certeza && bash infra/verificar_pdf_cabana.sh
   Si dice OK, el adaptador se puede desplegar (no desplegado, no pusheado).
B. git add tambien pidio aprobacion: el trabajo esta en disco SIN COMMIT
   (239 pruebas en verde). Para guardarlo:
     cd ~/certeza && git add certeza/adaptadores/pdf.py certeza/adaptadores/excel.py \
       certeza/api.py tests/_pdf_texto.py tests/test_adaptador_pdf.py \
       tests/test_camino_completo.py infra/verificar_pdf_cabana.sh \
       reportes/ultimo.txt reportes/pendiente.txt && \
     git commit -m "feat(pdf): adaptador de catalogos en PDF con texto; fix Expediente con dict de declarados"

=== 2026-09-24: auditoria de Cabana Emma y cola de subidas ===
A. DECISION: presupuesto en PDF (ver ultimo.txt). Opciones:
   1) Hoy, sin codigo: subir el Excel del que se imprimio el PDF (el PDF
      salio de "Microsoft: Print To PDF"). La auditoria corre de inmediato.
   2) Adaptador de PDF con texto (pypdf ya esta en requirements): 88/88
      conceptos en la prueba. Riesgo: el formato es el de PCA; otro
      proveedor puede traer otro acomodo. Un PDF escaneado seguiria sin
      leerse (eso si pediria OCR).
   Recomendacion: 1 ahora, y 2 como tarea aparte con pruebas sobre este y
   otros presupuestos reales. Aparte, el mensaje del 409 deberia decir "este
   archivo es PDF" en vez de "todavia no hay presupuesto".
B. DESPLEGAR: sigue sin desplegar el arreglo del 2026-09-23 y ahora tambien
   la cola persistente (bc771d9). El deploy fue bloqueado por permisos en
   esta sesion:
     cd ~/certeza && bash infra/deploy.sh
C. Duplicados: el presupuesto ya esta 4 veces en la obra (mismos bytes; la
   ultima copia, 2026-09-24 01:41 UTC). Refuerza el pendiente del sha256.

=== 2026-09-23 19:10: causa de las 221 fotos (ver ultimo.txt) ===
A. DESPLEGAR el arreglo (subidas de 3 en 3 + cierre de "esperando"
   vencidos + NotFound de GCS). No venia marcado [DESPLEGAR]:
     cd ~/certeza && bash infra/deploy.sh
   Por que: sin esto el navegador sigue lanzando todo a la vez y la
   siguiente subida masiva se puede perder igual.
B. Despues del deploy: abrir Cabana Emma (las 221 pasan a "fallido" con
   motivo) y volver a subir las fotos. Recomendacion: desde una computadora
   con la pestana abierta hasta que termine; el aviso de cierre ya lo pide.
C. Siguen en pie: duplicados por sha256 (Presupuesto x3, Concentrado x2).

=== 2026-09-23 (noche) ===
RESUELTO: region desplegada (certeza-api-00029-hpr); los 8 PDFs de
Cabana Emma en "listo". Ver ultimo.txt.

A. (resuelto por el arreglo de las 19:10: pasan a "fallido" al desplegar)
B. Duplicados: Presupuesto_Base (x3) y Concentrado_Avance (x2), con los
   mismos bytes. Decide si la subida debe detectar el sha256 repetido.
C. (propuesta, sigue en pie) Barrido que reencole documentos en
   "en_revision" viejos, y que marque "fallido" los "esperando" sin
   archivo despues de X horas. Asi este limbo no se vuelve a acumular.

=== NUEVO 2026-09-23: antivirus en certeza-escaner — que falta de tu lado ===
1. Probar una subida desde el navegador con tu cuenta real (un PDF). Yo
   probe la cadena desde la cola en adelante con EICAR y un PDF limpio
   (ver ultimo.txt), pero no puedo entrar con Firebase: el PUT del
   navegador y el login no los recorri.
2. Unas 180 fotos PXL_*.jpg de la obra o_cabana_emma estan en "esperando"
   desde el 2026-09-22: el navegador nunca termino de subirlas (no hay
   objeto en la cuarentena). No es el antivirus. Hay que volver a subirlas;
   no las toque.
3. DECISION (menor): definiciones de mas de 7 dias = el escaner responde
   "no pude revisar" en vez de "limpio" (umbral de ClamAV). Si el espejo
   falla una semana seguida, nadie sube nada hasta que se arregle.
   Alternativa: aprobar con definiciones viejas y solo alertar.
   Recomendacion: dejarlo como esta; Cloud Scheduler marca el trabajo
   certeza-espejo-clamav como fallido desde el primer dia y se ve en la
   consola.

=== RESUELTO 2026-09-22: audiencia OIDC del worker + CERTEZA_URL faltante ===
Ver reportes/ultimo.txt para el detalle. Desplegado (revision
certeza-api-00022-27w) y verificado contra recursos reales: tarea real en
la cola, 200 del worker; 4 intentos de llamar /worker/escanear sin
credenciales validas, los 4 con 403.

NOTA para otras sesiones: esta sesion SI tuvo gcloud autenticado (Cloud
Shell, cuenta abnercinplag@gmail.com) y pudo desplegar y probar contra
produccion. Si otra sesion reporta estar bloqueada por falta de
credenciales (como el diagnostico de subida de abajo), vale la pena
reintentar ese trabajo desde un entorno con sesion de gcloud activa antes
de darlo por imposible.

=== RESUELTO en codigo 2026-09-23 (falta deploy, ver A): GCP_REGION vs CERTEZA_REGION ===
infra/deploy.sh fija GCP_REGION en el servicio; certeza/config.py lee
CERTEZA_REGION. Hoy no causa nada visible porque los dos valores por
omision coinciden ("us-central1"), pero si algun dia se despliega a otra
region, CFG.region se quedaria en "us-central1" sin avisar (mismo patron
que el bug de CERTEZA_URL que se arreglo hoy). No lo toque para no meter
un cambio sin pedir: decidir si renombrar la variable en deploy.sh o leer
GCP_REGION tambien en config.py.

=== NUEVO 2026-09-21: subida sigue rota, diagnostico en vivo pendiente ===
Se pidio reproducir la cadena completa de subida (intencion -> PUT firmado
-> confirmar) eslabon por eslabon, sin adivinar, porque ya se parcho tres
veces sin encontrar la causa real.

Revise el codigo completo de la cadena (certeza/nube/almacenamiento.py,
certeza/api.py, web/agente.html, infra/instalar.sh): nada inconsistente a
simple vista. El Content-Type que firma el servidor y el que manda el
navegador vienen del mismo file.type; x-goog-content-length-range se firma
y se envia igual en ambos lados; el CORS de instalar.sh SI declara ese
encabezado en responseHeader; el CSP (connect-src) ya incluye
storage.googleapis.com (confirmado por infra/humo.sh en la corrida
anterior). Los dos defectos reales previos de esta misma cadena (el
AttributeError al firmar, el CSP bloqueando el PUT) solo se vieron
probando en vivo contra el servicio real, nunca leyendo el codigo. Este
probablemente es igual: sin poder ejecutar el PUT real no puedo afirmar
cual es la causa.

Escribi infra/diagnostico_subida.sh: firma una URL real con el codigo de
produccion, hace el PUT con curl imprimiendo el error literal de Google,
prueba variantes (sin x-goog-content-length-range, con otro Content-Type),
confirma con gcloud storage ls, y revisa CORS, el permiso de firmar
(iam.serviceAccountTokenCreator) y el desfase de reloj. Ya probe la logica
en modo memoria (sin nube): corre sin errores.

No lo pude correr contra el servicio real: este entorno de Claude Code no
tiene ninguna sesion de gcloud activa (gcloud auth list -> ninguna cuenta),
y aqui materializar o explorar credenciales esta bloqueado por politica
del sandbox en el que corro (no es algo que pueda rodear). Comando exacto
para correrlo, con las credenciales que SI reproducen exactamente la
identidad que usa produccion (la cuenta de servicio, no tu usuario —
EnGCS._firmar usa cred.service_account_email, que solo existe en
credenciales con sabor de cuenta de servicio):

  gcloud auth login
  gcloud auth application-default login \
    --impersonate-service-account=certeza-api@cinplag-v4-core.iam.gserviceaccount.com
  cd ~/certeza && ./infra/diagnostico_subida.sh cinplag-v4-core us-central1

(la ultima linea requiere que tu cuenta ya tenga roles/iam.serviceAccountTokenCreator
sobre esa cuenta de servicio para poder impersonarla; si no lo tiene, el
segundo comando lo dice explicitamente).

El log completo queda en reportes/diagnostico_subida_<fecha>.log. Si el
PUT con curl SI funciona ahi, el problema es del navegador (CORS) y el
script lo dice con esas palabras; si falla, el cuerpo de error de Google
queda completo en el log, sin recortar.

=== RESUELTO 2026-09-20 (tarde): la app ya no se cae en cada deploy ===
El punto 1 de aqui (403 para todos los visitantes) y el punto 3
(gcloud sin autenticar) ya no aplican:
  - infra/deploy.sh (con --allow-unauthenticated) esta commiteado y
    pusheado desde ayer (937ef52).
  - Corri infra/deploy.sh hoy: el binding allUsers -> run.invoker
    sigue ahi despues del deploy (lo verifique con
    `gcloud run services get-iam-policy`, no solo confiando en que
    el script no lo toco).
  - infra/humo.sh contra el servicio real: 0 fallas.
  - curl GET / -> 200, GET /entrar -> 200.
Detalle completo en reportes/ultimo.txt. Si esto vuelve a pasar en un
proximo deploy, es una regresion nueva, no la misma causa de antes.

=== 1. T1: no pude confirmar con un archivo REAL que llega a "listo" ===
No tengo forma de generar un token de sesion de un usuario real de
Firebase sin materializar credenciales, y esa categoria de accion
tambien esta bloqueada en mi entorno. Lo que si verifique con
evidencia real:
  - causa raiz exacta del atasco original (AttributeError firmando URLs
    con las credenciales de Cloud Run)
  - permiso IAM ya aplicado (roles/iam.serviceAccountTokenCreator en
    certeza-api@cinplag-v4-core.iam.gserviceaccount.com)
  - codigo nuevo desplegado (revision certeza-api-00016-tcc)
  - los 2 documentos que estaban atorados ya se marcaron "fallido" con
    motivo visible y boton de reintentar
Recomendacion: ya que el acceso publico esta resuelto, sube un archivo
de prueba desde tu cuenta real y confirma que llega a "listo". Si
quieres que yo lo haga, dime como puedo autenticarme sin que tengas
que darme un permiso amplio.

=== 2. DECISION DE NEGOCIO: relay de correo para el aviso de T4 ===
El aviso de "tu auditoria ya esta lista" (certeza/resumen.py,
armar_auditoria_lista + enviar_correo) ya esta escrito y probado, pero
enviar_correo() no manda nada sin los secretos certeza-smtp-host,
certeza-smtp-usuario, certeza-smtp-clave y certeza-smtp-remitente en
Secret Manager (hoy no existen). Sin ellos el codigo no falla: registra
"no se envio" y sigue. Opciones:
  a) SMTP de una cuenta de Gmail dedicada con contraseña de aplicacion.
     Gratis, rapido de armar, pero limites bajos de envio y el remitente
     se ve como Gmail, no como CERTEZA.
  b) Un relay transaccional (Mailgun, SendGrid, Amazon SES, etc.) por su
     SMTP normal (smtplib les sirve igual, no hace falta su SDK: sigue
     siendo "sin dependencias nuevas"). Mejor entregabilidad, remitente
     con dominio propio, mas confiable para algo que ve el cliente.
Mi recomendacion: (b), aunque sea el mas barato de esos servicios,
porque este correo es justo el que trae de vuelta a alguien que ya se
iba a olvidar de la herramienta: si cae en spam o se ve como spam, no
sirve. Decide cual y dame las credenciales; yo las subo a Secret
Manager con `gcloud secrets create` (comandos ya listos en
infra/instalar.sh como referencia del patron que ya se usa ahi).

=== 3. ACCION DE ABNER: correr POST /auditar de Cabana Emma contra produccion (2026-09-24) ===
El endpoint exige un ID token de Firebase de un miembro de e_certeza_883366.
No hay forma de sacarlo sin tu sesion: la alternativa seria acuñar un token
a tu nombre suplantando la cuenta de servicio, y eso no lo hago sin que lo
autorices. Ademas la auditoria escribe un corte en Firestore y una entrada
en bitacora a tu nombre.
Pasos:
  1) Abre https://certeza-api-823365164197.us-central1.run.app/app con tu
     sesion iniciada, DevTools > Consola, y corre:
       copy(await AUTH.currentUser.getIdToken(true))
     (queda el token en el portapapeles; dura 1 hora)
  2) En Cloud Shell:
       cd ~/certeza && TOKEN='<pega aqui>' && \
       curl -s -w '\n[HTTP %{http_code}]\n' -X POST \
         -H "Authorization: Bearer $TOKEN" \
         https://certeza-api-823365164197.us-central1.run.app/api/obras/o_cabana_emma_1eced8/auditar \
         | tee -a reportes/estado_deploy.txt
Nota: sin doc_id la API audita el documento "listo" mas reciente de la obra;
hoy es d_presupuesto_base_pca_02_06_2026_pdf_81c148 (el mismo que valido
infra/verificar_pdf_cabana.sh: 88 conceptos, 11 capitulos).

=== 4. DECISION TECNICA: bitacora con cadena rota por concurrencia (2026-09-24) ===
Bitacora.verificar('e_bolo_s_inc_42ff33') -> "cadena rota en el evento 53".
Causa: Encadenada.registrar lee la cadena y escribe sin transaccion; con
subidas en paralelo dos eventos toman el mismo n y el mismo hash_previo
(n repetidos: 53,66,69,84,114). Nadie altero nada, pero la cadena ya no
prueba integridad. Recomendacion: registrar dentro de una transaccion de
Firestore con un documento contador por empresa. Las cadenas ya bifurcadas
no se pueden "arreglar" sin reescribir eventos; propongo documentarlas y
verificar a partir del siguiente evento. No lo hice: toca el registro de
auditoria, prefiero tu visto bueno.
